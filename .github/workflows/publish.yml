name: Publish

env:
  HUSKY: 0
  CI: true

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to publish (e.g., v3.0.0)'
        required: true
        type: string

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      id-token: write

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.tag || github.ref }}
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '25'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --prefer-offline

      - name: Build type declarations
        run: npm run build:types

      - name: Build UMD bundles
        run: npm run build:umd

      - name: Verify build artifacts
        run: |
          echo "Checking build artifacts..."
          ls -lh dist/
          echo ""
          echo "Type declarations:"
          find lib -name "*.d.ts" -type f

      - name: Dry-run publish
        run: npm publish --dry-run

      - name: Publish to npm with provenance
        id: publish
        run: |
          echo "Publishing to npm with trusted publishing (OIDC)..."
          echo "npm version: $(npm --version)"
          npm publish --provenance
          echo "published=true" >> $GITHUB_OUTPUT

      - name: Verify npm publication
        if: steps.publish.outputs.published == 'true'
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          echo "Verifying publication..."
          sleep 5
          npm view "$PKG_NAME@$PKG_VERSION" version || echo "Package not yet available (may take a few minutes)"

      - name: Create language bundles archive
        run: |
          cd dist
          zip -r languages.zip languages/

      - name: Upload build artifacts to release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ inputs.tag || github.event.release.tag_name }}
          files: |
            dist/n2words.js
            dist/n2words.js.map
            dist/languages.zip

      - name: Generate job summary
        if: always()
        run: |
          PKG_VERSION=$(node -p "require('./package.json').version")
          echo "## Publish Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Version** | v${PKG_VERSION} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Tag** | \`${{ inputs.tag || github.event.release.tag_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${GITHUB_SHA::7}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Published** | ${{ steps.publish.outputs.published == 'true' && 'Yes' || 'No' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.publish.outputs.published }}" == "true" ]; then
            PKG_NAME=$(node -p "require('./package.json').name")
            echo "### Package Links" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- **npm**: https://www.npmjs.com/package/$PKG_NAME/v/${PKG_VERSION}" >> $GITHUB_STEP_SUMMARY
            echo "- **unpkg**: https://unpkg.com/$PKG_NAME@${PKG_VERSION}/" >> $GITHUB_STEP_SUMMARY
            echo "- **jsDelivr**: https://cdn.jsdelivr.net/npm/$PKG_NAME@${PKG_VERSION}/" >> $GITHUB_STEP_SUMMARY
          fi
