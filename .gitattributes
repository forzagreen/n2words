# Normalize all text files to LF line endings
# This ensures consistent line endings across Windows, macOS, and Linux
* text=auto eol=lf

# Lock files - mark as generated for GitHub Linguist
package-lock.json linguist-generated=true
