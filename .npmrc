# Enforce Node.js version from package.json engines field
engine-strict=true

# Use exact versions when adding dependencies (no ^ or ~)
save-exact=true

# Prefer offline installation from cache
prefer-offline=true
